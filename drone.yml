kind: pipeline
name: default

steps:
- name: nodeapp
  image: node
  volumes:
  - name: cache
    path: /
  commands:
  - npm install
  - yarn run build
  
  volumes:
- name: cache
  host:
    path: /var/www/vhosts/drone.battlecloud.space/vueapp
