kind: pipeline
name: default

steps:
- name: nodeapp
  image: node
  commands:
  - npm install
  - yarn run build
  volumes:
  - name: cache
    path: /tmp/cache
    
  volumes:
- name: node
  host:
    path: /tmp/cache:/var/lib/cache
