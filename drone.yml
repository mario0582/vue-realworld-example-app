kind: pipeline
name: default

steps:
- name: nodeapp
  image: node
  volumes:
  - name: cache
    path: /tmp/cache
  commands:
  - npm install
  - yarn run build
  
  volumes:
- name: cache
  host:
    path: /var/lib/cache
